import {type FC} from 'react';

const Header: FC = () => {
    return (
        <div className={'header-wrapper'}>
            <div className={'container'}>
                <h3>{'HEADER'}</h3>
            </div>
        </div>
    );
};

export default Header;